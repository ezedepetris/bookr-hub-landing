source 'https://rubygems.org'

gem 'sinatra'
gem 'puma'  # Better for production than thin
gem 'i18n'
gem 'stripe'  # For Stripe API integration

group :development do
  gem 'thin'    # Web server for development
  gem 'rerun'   # For automatic reloading during development
  gem 'dotenv'
end
